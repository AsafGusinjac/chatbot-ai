# Block direct web access to this directory.
# Apache 2.4 uses Require; 2.2 uses Order/Deny. Both are covered.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
